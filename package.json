{
  "name": "electron-auto-update-example",
  "version": "1.0.5",
  "description": "",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/tushar-tibude/auto-update-electron-ready.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "4.0.6",
    "electron-builder": "20.34.0"
  },
  "dependencies": {
    "electron-log": "^4.1.2",
    "electron-updater": "3.2.3"
  }
}
